{
  "name": "omega",
  "version": "1.0.0",
  "private": true,
  "description": "OMEGA — Plateforme pour expatriés à l'île Maurice (web + API + mobile)",
  "workspaces": [
    "backend",
    "frontend",
    "mobile"
  ],
  "scripts": {
    "install:all": "npm --prefix backend install && npm --prefix frontend install && npm --prefix mobile install",
    "dev:backend":  "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:mobile":   "npm --prefix mobile start"
  },
  "license": "UNLICENSED"
}
